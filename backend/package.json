{
  "name": "agendamento-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "npx cypress run --config-file ./cypress.config.js --headless"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.7.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "cypress": "^15.0.0"
  }
}
